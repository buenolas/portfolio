export function Footer() {
  return (
    <footer className="border-t border-white/10 px-4 py-8 md:px-6">
      <div className="mx-auto flex max-w-6xl flex-col justify-between gap-3 text-sm text-muted md:flex-row">
        <p>Lucas Bueno — Desenvolvedor backend, automações e IA aplicada.</p>
        <p className="font-mono text-xs uppercase tracking-[0.16em]">LB / DEV PROFILE</p>
      </div>
    </footer>
  );
}
